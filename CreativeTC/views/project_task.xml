<record id="view_todo_task_form_inherit_date" model="ir.ui.view">
    <field name="name">project.task.form.inherit.date</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_form2"/>  
    <!-- 👆 replace with the actual todo form external ID if it's custom -->
    <field name="arch" type="xml">
        <!-- Add the field after description -->
        <xpath expr="//field[@name='description']" position="after">
            <field name="date_assigned"/>
        </xpath>
    </field>
</record>
